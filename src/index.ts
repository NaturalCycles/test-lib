import { deepFreeze, silentConsole } from './testing.util'
import { mockTime, mockTimeMillis, resetTime } from './time.util'
import * as timemachine from 'timemachine'

export {
  deepFreeze,
  silentConsole,
  mockTime,
  mockTimeMillis,
  resetTime,
  timemachine,
}
